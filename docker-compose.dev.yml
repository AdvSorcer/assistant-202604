services:
  api:
    environment:
      Cors__AllowedOrigins__0: http://localhost:8080
    ports:
      - "5251:8080"

  frontend:
    build:
      args:
        VITE_API_BASE_URL: http://localhost:5251/api
    ports:
      - "8080:80"
